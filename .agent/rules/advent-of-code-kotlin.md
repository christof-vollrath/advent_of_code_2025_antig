---
trigger: always_on
---

- Use Kotlin as the main programming language
- Use Gradle as the build system with kotlin kts
- Use Java 21 
- Strictly follow a TDD style developement
- Use kotest as a test framework
- Use behavior spec as the main testing styles
- For behavior spec use Given, When, Then with camel case
- When tests contains many input data elements use data driven tests
- Write code and tests in the same file and bundle them for each advent of code day
- Combine also part 1 and part 2 in the same file
- For common utility functions check what is available in Common.kt and use it.
- If you identify a common general need for a functionality put it to Common.kt
